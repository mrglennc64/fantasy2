__pycache__/
*.pyc
.venv/
.env
credentials.json
*.log
.DS_Store
web/dist/
# runtime state — the paper track record is owned by whichever host runs the
# cron (kv8); committing it would fight the daily writes on git pull.
data/pick6_entries.csv
